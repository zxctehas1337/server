# Desktop Electron app environment configuration
NODE_ENV=desktop
DESKTOP=true

# Database configuration (SQLite for desktop)
DATABASE_TYPE=sqlite
SQLITE_DB_PATH=./data/kracken.db

# Server configuration
PORT=3000

# App metadata
APP_NAME=Kracken Messenger
APP_VERSION=1.0.0
